import { RPNCalculator } from "../../classes/RPNCalculator";
import { CLICalculator } from "../../classes/CLICalculator";

// ToDo: add an integration test for the CLI version of the calculator
test("CLICalculator class sanity check", () => {
    expect(true).toEqual(true);
})